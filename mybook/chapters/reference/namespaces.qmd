# 네임스페이스

`wildcards` 네임스페이스는 **규칙 내부에서만** 사용 가능합니다.
이는 와일드카드가 개별 규칙 안에서만 존재하며,
규칙 간에 공유되지 않기 때문입니다!

즉, 다음과 같이 `rule` 블록 바깥에서는 `wildcards`를 참조할 수 없습니다.
와일드카드는 항상 해당 규칙의 `input:`, `output:`, `shell:` 등의
블록 안에서만 `{wildcards.이름}` 형태로 접근할 수 있습니다.

관련 예제: `code/examples/wildcards.namespace`
